#include <iostream>
using namespace std;
template <class Type>Type Abs(Type s){ //原来这里重复定义了，应该去掉。
	if(s>=0){
		s=s;
	}else{
		s=0-s;
	} 
	return s; 
}

 int main() { 
	int a; 
	cout<<"输入一个数："<<endl; 
	cin>>a;
	a=Abs(a); 
	cout<<"它的绝对值是: "<<a<<endl; 
	return 0; 
}
